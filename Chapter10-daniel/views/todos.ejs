<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
    <title><%= title %></title>
</head>
<body>
    <h1><%= title %></h1>

    <form class="form-inline" action='/todos/add' method="POST">
        <input type="text" class="form-control mb-2 mr-sm-2" name="new-todo">      
        <button type="submit" class="btn btn-primary mb-2">
            Add Todo
        </button>
    </form>

    <% if (error) { %>
        <div class="alert alert-danger" role="alert">
            <%= error %>
        </div>
    <% } %> 

    <ul class="list-group">
        <% for (let i = 0; i < todoObjects.length; i++){ %>
         <li class="list-group-item d-flex justify-content-between align-items-center">
            <a href="/todos/<%= todoObjects[i].getId() %>"><%= todoObjects[i].getName() %></a>
             <form action='/todos/delete/<%= todoObjects[i].getId() %>' method="POST">
                <button type="submit" class="btn btn-primary mb-2">
                    Delete
               </button>
            </form> 
        </li> 
        <% } %>
    </ul>
</body>
</html>